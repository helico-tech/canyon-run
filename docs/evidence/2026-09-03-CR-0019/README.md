# CR-0019 evidence — lava rift biome

`node tools/headless/run.ts --seed 8 --skip 840 --frames 240 --every 40 --out runs/cr19`
(seed 8's first special segment is the lava rift). All gates ok.

`lava-sheet.png`: deep near-vertical slot, horizontal strata with orange seams,
a flat bright lava floor with rock islands, arch bridges across the rift, dark
red fog. The hemisphere "sky" colour is the lava glow so up-facing surfaces
burn without a custom emissive shader.
