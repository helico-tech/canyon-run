[package]
name = "field"
version = "0.1.0"
edition = "2021"
description = "Canyon Run density field, bit-identical to src/terrain (ADR 0009)"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
debug = false
